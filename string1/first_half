# Given a string of even length, return the first half. So the string "WooHoo" yields "Woo".

def first_half(str):
    length = len(str)
    if length % 2 == 0:
        return str[0:length/2]
    else:
        return "String not even length."
